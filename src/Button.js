
const Button = ({onClick}) => {
    
    return (
        <button 
            onClick = {handlingClick} 
            className = 'wrap'
        > 
            DASHBOARD 
        </button>
    )
}
export default Button
